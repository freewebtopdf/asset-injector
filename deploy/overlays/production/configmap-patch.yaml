apiVersion: v1
kind: ConfigMap
metadata:
  name: asset-injector-config
data:
  log-level: "info"
  cache-max-size: "50000"
  cache-ttl: "1h"
  cors-origins: "https://api.example.com,https://admin.example.com"
